{
    "defaults": {
        "timeout": 30000,
        "chromeLaunchConfig": {
            "args": [
                "--no-sandbox",
                "--disable-setuid-sandbox",
                "--disable-dev-shm-usage",
                "--disable-gpu",
                "--no-first-run",
                "--no-zygote",
                "--single-process"
            ]
        }
    },
    "urls": [
        "http://localhost:3000/",
        "http://localhost:3000/appareils",
        "http://localhost:3000/reseaux"
    ]
}